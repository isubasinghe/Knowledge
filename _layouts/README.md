# \_layouts

Jekyll HTML layouts used by the `jekyll-theme-hacker` theme (`_config.yml`). Not knowledge content — site scaffolding only.

- `home.html` — landing-page layout.
- `page.html` — layout for individual pages.
