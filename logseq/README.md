# logseq

Logseq configuration for this graph. Not knowledge content — tool config only.

- `config.edn` — Logseq settings (graph behaviour, shortcuts, etc.).
- `custom.css` — Logseq UI style overrides.
